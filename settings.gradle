pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'binaries'

def toolsPath = System.getenv('CI') ? 'tools' : '../tools'
includeBuild(toolsPath)