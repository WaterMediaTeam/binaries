pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'binaries'

def toolsPath = file('tools').exists() ? 'tools' :
        file('../tools').exists() ? '../tools' :
                null
includeBuild(toolsPath)